# Base de l'API. En CI, la valeur vient du secret GitHub VITE_API_BASE.
# En local, copier ce fichier en .env.production (ou .env.local pour le dev).
VITE_API_BASE=https://backend.willyaccessoire.com
